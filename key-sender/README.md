> **Language**: **English** | [한국어 (Korean)](README.ko.md)
> 
> Web application for transmitting API keys to BYD Sub AI via acoustic waves.

# BYD Sub AI Key Sender

This is a Vite + React + TypeScript web application that generates and transmits GGwave sound signals to configure API keys on the **BYD Sub AI** Android application.

## Run Locally
1. Install dependencies:
   ```bash
   npm install
   ```
2. Run the local development server:
   ```bash
   npm run dev
   ```

## Production Build
To build the application for deployment:
```bash
npm run build
```
